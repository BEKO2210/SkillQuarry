name: RanGate Evaluation

on:
  push:
    branches:
      - test/rangate-pro-20260814
    paths:
      - 'experiments/rangate/**'
      - '.github/workflows/rangate-eval.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  rust-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: experiments/rangate
    steps:
      - uses: actions/checkout@v4
      - name: Toolchain
        run: |
          rustc --version
          cargo --version
      - name: Format check
        run: cargo fmt --check
      - name: Cargo check
        run: cargo check --all-targets
      - name: Strict unsafe-op check
        env:
          RUSTFLAGS: -Dunsafe_op_in_unsafe_fn
        run: cargo check --all-targets
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Unit tests
        run: cargo test --all-targets -- --nocapture
      - name: Compile-fail API proofs
        run: cargo test --doc
      - name: Release-mode stress
        run: cargo test --release --all-targets
